# OS / editor
.DS_Store
.idea/
.vscode/

# Secrets / credentials
.env
.env.*
!.env.example
data/tokens.json
*.pem
*.key

# Build / runtime artifacts
dist/
build/
out/
tmp/
*.zip

# Live-test scratch produced when an agent runs the SKILL.md against
# a real Feishu tenant (see feishu-sqb-onboarding/SKILL.md Step 5 / 6).
# These are intermediate render outputs (proposal markdown, card JSON,
# zip staging) — never to be committed.
.live-*
.staging-*

# Python (in case examples generate scratch files)
__pycache__/
*.pyc
.venv/

# Node (generated artifacts only — no node_modules expected)
node_modules/
*.log
